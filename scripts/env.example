# ── Model weights ──────────────────────────────────────────────────
# Qwen3-14B model weights directory (config.json + safetensors + tokenizer.json).
# Set this to the absolute path where your model weights are stored.
MODEL_DIR="/path/to/Qwen3-14B-weights"
